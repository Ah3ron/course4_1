# Система оценки риска банкротства

Полнофункциональная система для оценки риска банкротства компаний с использованием финансовых моделей Альтмана и Таффлера. Проект включает FastAPI backend и SvelteKit frontend с современным пользовательским интерфейсом.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Установка и запуск](#установка-и-запуск)
- [Использование](#использование)
- [Архитектура](#архитектура)
- [Документация API](#документация-api)
- [Финансовые модели](#финансовые-модели)
- [Конфигурация](#конфигурация)
- [Разработка](#разработка)
- [Docker](#docker)
- [Тестирование](#тестирование)

## 🎯 Описание проекта

Система позволяет оценить риск банкротства компании на основе финансовых показателей, используя две проверенные модели:

- **Модель Альтмана (Z-score)** - классическая модель для прогнозирования банкротства
- **Модель Таффлера** - модель прогнозирования банкротства для различных отраслей

Система предоставляет комбинированную оценку риска на основе результатов обеих моделей.

## 🏗️ Структура проекта

```
.
├── backend/              # FastAPI сервер
│   ├── app/
│   │   ├── main.py      # Точка входа приложения
│   │   ├── models.py    # Pydantic модели для валидации
│   │   ├── api/         # API endpoints
│   │   │   ├── routes.py
│   │   │   └── schemas.py
│   │   └── financial_models/  # Модули финансовых моделей
│   │       ├── config.py
│   │       ├── financial_models.py
│   │       └── model.py
│   ├── config.py        # Конфигурация приложения
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/            # SvelteKit приложение
│   ├── src/
│   │   ├── routes/      # Страницы приложения
│   │   │   ├── +page.svelte
│   │   │   └── +layout.svelte
│   │   └── lib/         # API клиент и stores
│   │       ├── api.ts
│   │       └── stores.ts
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml    # Docker Compose конфигурация
└── README.md
```

## 🚀 Быстрый старт

### Шаг 1: Запуск Backend

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на http://localhost:8000

Проверьте работу: http://localhost:8000/api/health

### Шаг 2: Запуск Frontend

Откройте еще один терминал:

```bash
cd frontend
npm install
npm run dev
```

Frontend будет доступен на http://localhost:5173

### Готово! 🎉

Откройте браузер и перейдите на http://localhost:5173

Заполните форму финансовыми показателями компании и получите оценку риска банкротства.

## 💻 Установка и запуск

### Предварительные требования

- **Python 3.11+**
- **Node.js 20+**
- **pip** и **npm**

### Детальная установка

#### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите сервер:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу: http://localhost:8000

#### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev сервер:
```bash
npm run dev
```

Frontend будет доступен по адресу: http://localhost:5173

## 📱 Использование

1. Откройте frontend в браузере (http://localhost:5173)
2. Заполните форму с финансовыми показателями компании:

   **Для модели Альтмана:**
   - Оборотный капитал (текущие активы - текущие обязательства)
   - Общие активы
   - Нераспределенная прибыль
   - EBIT (прибыль до уплаты процентов и налогов)
   - Рыночная стоимость собственного капитала
   - Общие обязательства
   - Выручка

   **Для модели Таффлера:**
   - Прибыль до налогообложения
   - Текущие обязательства
   - Текущие активы
   - Операционная прибыль

3. Нажмите "Оценить риск банкротства"
4. Просмотрите результаты обеих моделей и комбинированную оценку

## 🏛️ Архитектура

### Финансовые модели

- **Модель Альтмана (Z-score)**: Классическая модель для прогнозирования банкротства
- **Модель Таффлера**: Модель прогнозирования банкротства для различных отраслей
- **Комбинированная оценка**: Агрегирование результатов обеих моделей

### Backend (FastAPI)

- **Endpoints**:
  - `POST /api/predict` - Оценка риска банкротства
  - `GET /api/health` - Проверка здоровья сервера
  - `GET /api/model-info` - Информация о моделях
- **Валидация**: Pydantic модели для строгой валидации данных
- **CORS**: Настроен для frontend
- **Логирование**: Структурированное логирование с настраиваемым уровнем

### Frontend (SvelteKit)

- **UI Framework**: DaisyUI + Tailwind CSS
- **Состояние**: Svelte stores для управления состоянием
- **История**: Локальное хранение (localStorage)
- **Темы**: Светлая/темная тема с переключением
- **Адаптивность**: Полностью адаптивный дизайн

## 📡 Документация API

### POST /api/predict

Оценка риска банкротства компании.

**Запрос:**
```json
{
  "working_capital": 500000,
  "total_assets": 2000000,
  "retained_earnings": 300000,
  "ebit": 400000,
  "market_value_equity": 1500000,
  "total_liabilities": 500000,
  "sales": 3000000,
  "profit_before_tax": 350000,
  "current_liabilities": 200000,
  "current_assets": 700000,
  "operating_income": 380000
}
```

**Ответ:**
```json
{
  "altman_z_score": 3.4567,
  "altman_risk_level": "low",
  "altman_recommendation": "Низкий риск банкротства. Компания находится в безопасной зоне.",
  "taffler_z_score": 0.5234,
  "taffler_risk_level": "low",
  "taffler_recommendation": "Низкий риск банкротства. Финансовое положение компании стабильное.",
  "combined_risk_level": "low",
  "combined_recommendation": "Обе модели показывают низкий риск банкротства. Компания находится в стабильном финансовом положении."
}
```

### GET /api/health

Проверка здоровья сервера.

**Ответ:**
```json
{
  "status": "healthy",
  "message": "Bankruptcy Risk Assessment API is running",
  "timestamp": "2024-01-01T12:00:00"
}
```

### GET /api/model-info

Информация о доступных финансовых моделях.

**Ответ:**
```json
{
  "models": ["altman", "taffler"],
  "altman_description": "Модель Альтмана (Z-score) - классическая модель...",
  "taffler_description": "Модель Таффлера - модель прогнозирования...",
  "required_fields": {
    "altman": ["working_capital", "total_assets", ...],
    "taffler": ["profit_before_tax", "current_liabilities", ...]
  }
}
```

### Интерактивная документация

После запуска backend, интерактивная документация API доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 📊 Финансовые модели

### Модель Альтмана (Z-score)

Разработана Эдвардом Альтманом в 1968 году. Использует 5 финансовых коэффициентов для оценки вероятности банкротства компании в течение 2 лет.

**Формула:**
```
Z = 1.2*X1 + 1.4*X2 + 3.3*X3 + 0.6*X4 + 1.0*X5
```

Где:
- X1 = Working Capital / Total Assets
- X2 = Retained Earnings / Total Assets
- X3 = EBIT / Total Assets
- X4 = Market Value of Equity / Total Liabilities
- X5 = Sales / Total Assets

**Интерпретация:**
- **Z > 2.99**: Безопасная зона (низкий риск банкротства)
- **1.81 < Z < 2.99**: Серая зона (средний риск)
- **Z < 1.81**: Зона опасности (высокий риск банкротства)

### Модель Таффлера

Разработана Ричардом Таффлером. Использует 4 финансовых коэффициента и показывает эффективность в различных отраслях.

**Формула:**
```
Z = 3.2 + 12.18*X1 + 2.50*X2 - 10.68*X3 + 0.029*X4
```

Где:
- X1 = Profit before Tax / Current Liabilities
- X2 = Current Assets / Total Liabilities
- X3 = Current Liabilities / Total Assets
- X4 = (Operating Income / Total Assets) * 100

**Интерпретация:**
- **Z > 0.2**: Низкий риск банкротства
- **0.0 < Z < 0.2**: Средний риск
- **Z < 0.0**: Высокий риск банкротства

### Комбинированная оценка

Система объединяет результаты обеих моделей, выбирая худший (наибольший) уровень риска для финальной оценки. Это обеспечивает более консервативный и надежный подход к оценке риска банкротства.

## ⚙️ Конфигурация

### Backend

Настройки в `backend/config.py` или через переменные окружения:

- `API_HOST` - хост сервера (по умолчанию: 0.0.0.0)
- `API_PORT` - порт сервера (по умолчанию: 8000)
- `API_RELOAD` - автоматическая перезагрузка при изменениях (по умолчанию: true)
- `LOG_LEVEL` - уровень логирования (по умолчанию: INFO)
- `CORS_ORIGINS` - разрешенные источники для CORS (разделенные запятыми)

Пример использования переменных окружения:

```bash
export API_PORT=8001
export LOG_LEVEL=DEBUG
uvicorn app.main:app --reload
```

### Frontend

Переменные окружения (создайте файл `.env` в директории frontend):

- `VITE_API_URL` - URL backend API (по умолчанию: http://localhost:8000)

Пример `.env` файла:

```env
VITE_API_URL=http://localhost:8000
```

## 🔧 Разработка

### Структура кода

#### Backend

- `app/main.py` - главный файл приложения, настройка FastAPI
- `app/models.py` - Pydantic модели для валидации данных
- `app/api/routes.py` - API endpoints
- `app/financial_models/financial_models.py` - реализация финансовых моделей
- `app/financial_models/model.py` - обертка для работы с моделями
- `config.py` - конфигурация приложения

#### Frontend

- `src/routes/+page.svelte` - главная страница с формой
- `src/routes/+layout.svelte` - общий layout приложения
- `src/lib/api.ts` - API клиент для взаимодействия с backend
- `src/lib/stores.ts` - Svelte stores для управления состоянием

### Добавление новых финансовых моделей

1. Добавьте функцию расчета в `backend/app/financial_models/financial_models.py`
2. Обновите `backend/app/financial_models/model.py` для использования новой модели
3. Обновите `backend/app/models.py` для новых полей данных
4. Обновите `frontend/src/lib/api.ts` и форму в `frontend/src/routes/+page.svelte`

### Стиль кода

- **Backend**: PEP 8, type hints, docstrings
- **Frontend**: Prettier + ESLint для форматирования

## 🐳 Docker

### Запуск с Docker Compose

Для запуска всего стека (backend + frontend) используйте Docker Compose:

```bash
docker-compose up --build
```

Это запустит:
- Backend на http://localhost:8000
- Frontend на http://localhost:5173

### Отдельные Docker контейнеры

#### Backend

```bash
cd backend
docker build -t bankruptcy-backend .
docker run -p 8000:8000 bankruptcy-backend
```

#### Frontend

```bash
cd frontend
docker build -t bankruptcy-frontend .
docker run -p 5173:5173 bankruptcy-frontend
```

## 🧪 Тестирование

### Тест API через curl

```bash
curl -X POST http://localhost:8000/api/predict \
  -H "Content-Type: application/json" \
  -d '{
    "working_capital": 500000,
    "total_assets": 2000000,
    "retained_earnings": 300000,
    "ebit": 400000,
    "market_value_equity": 1500000,
    "total_liabilities": 500000,
    "sales": 3000000,
    "profit_before_tax": 350000,
    "current_liabilities": 200000,
    "current_assets": 700000,
    "operating_income": 380000
  }'
```

### Ожидаемый ответ

```json
{
  "altman_z_score": 3.4567,
  "altman_risk_level": "low",
  "altman_recommendation": "Низкий риск банкротства. Компания находится в безопасной зоне.",
  "taffler_z_score": 0.5234,
  "taffler_risk_level": "low",
  "taffler_recommendation": "Низкий риск банкротства. Финансовое положение компании стабильное.",
  "combined_risk_level": "low",
  "combined_recommendation": "Обе модели показывают низкий риск банкротства. Компания находится в стабильном финансовом положении."
}
```

### Проверка здоровья API

```bash
curl http://localhost:8000/api/health
```

## 🔍 Рефакторинг Backend

Backend был рефакторирован для улучшения структуры и организации кода:

- ✅ Улучшена структура конфигурации с использованием типов
- ✅ Улучшена организация импортов
- ✅ Добавлены type hints для всех функций
- ✅ Улучшена обработка ошибок и логирование
- ✅ Использование констант вместо магических чисел
- ✅ Улучшена читаемость кода и документация
- ✅ Централизованная конфигурация через `config.py`

## 🐛 Возможные проблемы

### CORS ошибки

Если frontend не может подключиться к backend:

1. Убедитесь, что backend запущен на порту 8000
2. Проверьте настройки CORS в `backend/config.py`
3. Убедитесь, что URL frontend добавлен в `CORS_ORIGINS`

### Порт занят

Если порт 8000 или 5173 занят:

- Для backend: измените порт в команде запуска `--port 8001` или через переменную окружения `API_PORT`
- Для frontend: измените порт в `vite.config.js` или используйте флаг `--port`

### Ошибки импорта

Если возникают ошибки импорта в backend:

- Убедитесь, что вы находитесь в правильной директории
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
- Убедитесь, что используется правильная версия Python (3.11+)

## 📚 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SvelteKit документация](https://kit.svelte.dev/)
- [DaisyUI документация](https://daisyui.com/)
- [Altman Z-score модель](https://en.wikipedia.org/wiki/Altman_Z-score)
- [Taffler модель](https://en.wikipedia.org/wiki/Taffler_model)

## 📄 Лицензия

Этот проект создан в образовательных целях для курсовой работы.

## 👤 Автор

Система оценки риска банкротства - Курсовая работа 2024

---

**Версия:** 2.0.0

